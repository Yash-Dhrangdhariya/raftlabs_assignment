query GetAllUsers($exceptUser: String!) {
    getUsersExcept(exceptUser: $exceptUser) {
        _id
        name
        userId
        email
        avatar
        news
        followings
        followers
    }
}